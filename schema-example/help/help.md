# description

Show help message and usage information for all available commands.

# options
```json
[
    {
        "flags": "--format",
        "description": "Output format (text, json, markdown)",
        "type": "string",
        "default": "text"
    }
]
```

# ARGUMENTS
```json
[]
```

# Settings
```json
{}
``` 